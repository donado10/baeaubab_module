import EcritureDigitaleSection from '@/features/digitale/ecritures/components/EcritureDigitaleSection'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <><EcritureDigitaleSection /></>
    )
}

export default page